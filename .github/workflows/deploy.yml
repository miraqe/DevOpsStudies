name: Deploy On Merge

on:
  push:
    branches:
      - testing_environment
  pull_request:
    types:
      - closed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Log Deployment Message
        run: echo "Deployed!"
